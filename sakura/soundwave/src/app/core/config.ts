/**
 * Dev-only config — no environment.ts/fileReplacements wired up yet since there's only one
 * deploy target so far. Revisit once soundwave needs a real prod build.
 */
export const CONTROL_TOWER_URL = 'http://localhost:8080';
export const NORTHSTAR_URL = 'http://localhost:3000';
